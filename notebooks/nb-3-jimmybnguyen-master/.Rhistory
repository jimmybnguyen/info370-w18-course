# Load data
grades = read.csv("grades.csv")
grades
gender <- grades %>%
group_by(sex) %>%
# How many students are there of each sex?
gender <- grades %>%
group_by(sex) %>%
gender
library(dplyr)
library(tidyr)
gender <- grades %>%
group_by(sex) %>%
# How many students are there of each sex?
gender <- grades %>%
group_by(sex) %>%
gender
gender <- grades %>%
group_by(sex) %>%
gender
gender <- grades %>%
group_by(sex)
gender
# How many students are there of each sex?
males <- filter(grades, sex="m")
# How many students are there of each sex?
males <- filter(grades, sex=="m")
females <- filter(grades, sex=="f")
males
nrows(males)
nrow(males)
nrow(females)
means <- grades %>%
group_by(sex) %>%
summarise_all(mean)
means
# Create a scatter plot showing the average female score (x) v.s. male score (y)
# Add a 45 degree line to the graph
# Hint: this requires reshaping
reshaped <- gather(means[3:11],'assignment','score',na.rm = TRUE, convert=FALSE, factor_key=TRUE)
reshaped
means
# Create a scatter plot showing the average female score (x) v.s. male score (y)
# Add a 45 degree line to the graph
# Hint: this requires reshaping
reshaped <- gather(means[1:10],'assignment','score',na.rm = TRUE, convert=FALSE, factor_key=TRUE)
reshaped
# Create a scatter plot showing the average female score (x) v.s. male score (y)
# Add a 45 degree line to the graph
# Hint: this requires reshaping
reshaped <- gather(means[3:10],'assignment','score',na.rm = TRUE, convert=FALSE, factor_key=TRUE)
reshaped
# Create a scatter plot showing the average female score (x) v.s. male score (y)
# Add a 45 degree line to the graph
# Hint: this requires reshaping
#reshaped <- gather(means[3:10],'assignment','score',na.rm = TRUE, convert=FALSE, factor_key=TRUE)
gender_scores <- means[-c(2, 11, 12)]
gender_scores
means
# Gather columns 2:9 into new columns of "assignment" and "value"
gender_scores <- gather(gender_scores, assignment, value, 2:9)
gender_scores
spread_gender_scores <- spread(gender_scores, sex, score)
spread_gender_scores <- spread(gender_scores, sex, grades)
spread_gender_scores <- spread(gender_scores, sex, value)
spread_gender_scores
gender_scores
gender_scores <- gather(gender_scores, assignment, score, 2:9)
spread_gender_scores <- spread(gender_scores, sex, score)
# Gather columns 2:9 into new columns of "assignment" and "value"
gender_scores <- gather(gender_scores, assignment, score, 2:9)
# Gather columns 2:9 into new columns of "assignment" and "value"
gender_scores <- gather(gender_scores, assignment, value, 2:9)
gender_scores <- gather(gender_scores, assignment, value, 2:9)
spread_gender_scores <- spread(gender_scores, sex, value)
gender_scores
spread_gender_scores
library(ggplot2)
attach(spread_gender_scores)
plot()
attach(spread_gender_scores)
plot(f,m)
ggplot(spread_gender_scores, aes(x=f, y=m)) + geom_point(size=2, shape=23) + geom_smooth(method=1m)
ggplot(spread_gender_scores, aes(x=f, y=m)) +
geom_point(size=2, shape=23) +
geom_smooth(method=1m)
ggplot(spread_gender_scores, aes(x=f, y=m)) +
geom_point(size=2, shape=23)
ggplot(spread_gender_scores, aes(x=f, y=m)) +
geom_point(size=2, shape=23) +
geom_smooth(method=lm)
# Perform a T test on the "total" grade.
male_total_scores <- grades %>% filter(sex == "m") %>% select(total)
male_total_scores
female_total_scores <- grades %>% filter(sex == "f") %>% select(total)
t.test(male_total_scores, female_total_scores)
t.test(female_total_scores,male_total_scores)
